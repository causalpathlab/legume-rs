// All feature-pair helpers formerly lived here: they supported the dense
// feature-pair profile path which was replaced by the feature-module + sparse
// module-pair residual path in `profiles.rs`. Keep the module present (it
// is registered in `feature_network/mod.rs`) but intentionally empty so the
// directory structure stays stable.
