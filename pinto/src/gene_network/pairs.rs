// All gene-pair helpers formerly lived here: they supported the dense
// gene-pair profile path which was replaced by the gene-module + sparse
// module-pair residual path in `profiles.rs`. Keep the module present (it
// is registered in `gene_network/mod.rs`) but intentionally empty so the
// directory structure stays stable.
