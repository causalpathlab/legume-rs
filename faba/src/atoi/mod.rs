//! `faba atoi` — A-to-I editing site calling.
//!
//! The subcommand entry only; the read-level machinery it drives lives in
//! [`crate::editing`], shared with [`crate::m6a`].

/// The `faba atoi` run. Binary entry: [`run::run_atoi`].
pub mod run;
